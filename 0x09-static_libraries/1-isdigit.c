#include "main.h"

/**
 * _isdigit - this function checks for a digits 0 - 9
 * @c: integer value being checked
 * Return: 1 if c is a digit, else 0
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
